<!DOCTYPE html>
<html>
  <%= render 'head' %>
  <body>
    <div class="container">
      <%= render 'components/nav' %>
      <%= render 'components/loader' %>
      <%= yield %>
      <%= render 'footer' %>
      <%= render 'handlebars' %>
    </div>
  </body>
</html>
